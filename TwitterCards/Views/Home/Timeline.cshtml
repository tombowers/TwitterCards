@model TwitterCards.App.Home.ViewModels.TimelineViewModel

<h2>Timeline</h2>

@foreach (var tweet in Model.TimelineTweets)
{
	<p>
		<strong>
			@tweet.Author
		</strong>
		<br />
		@tweet.Text
	</p>
}